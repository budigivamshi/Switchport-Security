# Switchport-Security
A Switchport Security project focuses on configuring port security on Layer 2 switches to restrict unauthorized devices from accessing the network. It involves setting a maximum number of allowed MAC addresses per port and defining violation modes like shutdown, restrict, or protect. The goal is to enhance network security by preventing MAC attacks
